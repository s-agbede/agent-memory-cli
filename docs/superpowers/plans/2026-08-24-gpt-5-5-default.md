# GPT-5.5 Default Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `gpt-5.5` the tested and documented default model while retaining the `OPENAI_MODEL` override.

**Architecture:** Change the single Pydantic settings default and align its contract test and current setup documentation. Do not change explicit model values in agent unit-test fixtures or rewrite historical plans and specifications.

**Tech Stack:** Python 3.12, Pydantic Settings, pytest, Ruff, mypy, uv

---

### Task 1: Runtime and Configuration Contract

**Files:**
- Modify: `tests/test_config.py:22`
- Modify: `src/trip_agent/config.py:13`

- [x] **Step 1: Change the default assertion first**

```python
assert settings.openai_model == "gpt-5.5"
```

- [x] **Step 2: Run the focused test and confirm RED**

Run:

```bash
uv run pytest -q tests/test_config.py::test_settings_load_required_values_and_defaults
```

Expected: failure showing actual `gpt-5.6-luna` and expected `gpt-5.5`.

- [x] **Step 3: Change the runtime default**

```python
openai_model: str = "gpt-5.5"
```

- [x] **Step 4: Run the focused test and confirm GREEN**

Run the Step 2 command again.

Expected: pass.

### Task 2: Current Setup Documentation

**Files:**
- Modify: `.env.example:2`
- Modify: `README.md:56-64`

- [x] **Step 1: Align the sample environment**

```dotenv
OPENAI_MODEL=gpt-5.5
```

- [x] **Step 2: Align the README**

Use `OPENAI_MODEL=gpt-5.5` in the sample and state that `gpt-5.5` is the default. Preserve the
existing explanation that the environment variable is optional.

- [x] **Step 3: Confirm current sources no longer advertise the previous default**

Run:

```bash
rg -n "gpt-5\.6-luna" src tests/test_config.py .env.example README.md
```

Expected: no matches. Explicit model values in unrelated agent test fixtures are outside this
check's scope and remain unchanged.

### Task 3: Verification and Commit

**Files:**
- Modify: `docs/superpowers/plans/2026-08-24-gpt-5-5-default.md`

- [x] **Step 1: Run complete verification**

Run each command separately:

```bash
uv run pytest -ra
uv run ruff format --check .
uv run ruff check .
uv run mypy src
git diff --check
```

Expected: all unit tests pass with only the opt-in Redis integration test skipped; formatting,
linting, typing, and whitespace checks exit successfully.

- [x] **Step 2: Commit the aligned default**

```bash
git add src/trip_agent/config.py tests/test_config.py .env.example README.md \
  docs/superpowers/plans/2026-08-24-gpt-5-5-default.md
git commit -m "chore: make gpt-5.5 the default model"
```
